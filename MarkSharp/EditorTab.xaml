<UserControl x:Class="MarkSharp.EditorTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MarkSharp"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0">
            <ui:SimpleStackPanel DockPanel.Dock="Top" Margin="10,5,0,0">
                <TextBlock Text="MARKDOWN" Style="{StaticResource TitleTextBlockStyle}" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}" />
            </ui:SimpleStackPanel>

            <TextBox x:Name="MarkdownTextBox"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     AllowDrop="True"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     BorderThickness="0"
                     FontFamily="Consolas"
                     FontSize="14"
                     Padding="10"
                     VerticalScrollBarVisibility="Visible"
                     ScrollViewer.ScrollChanged="MarkdownTextBox_ScrollChanged"
                     TextChanged="MarkdownTextBox_TextChanged" />
        </DockPanel>

        <GridSplitter Grid.Column="1"
                      Width="5"
                      HorizontalAlignment="Stretch"
                      Background="{DynamicResource SystemControlBackgroundBaseLowBrush}" />

        <DockPanel Grid.Column="2">
            <ui:SimpleStackPanel DockPanel.Dock="Top" Margin="10,5,0,0">
                <TextBlock Text="PREVIEW" Style="{StaticResource TitleTextBlockStyle}" Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}" />
            </ui:SimpleStackPanel>

            <wv2:WebView2 x:Name="PreviewBrowser"
                          WebMessageReceived="HandlePreviewScrollMessage" />
        </DockPanel>
    </Grid>
</UserControl>
